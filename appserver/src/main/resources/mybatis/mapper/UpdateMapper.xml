<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.audio.electric.mapper.UpdateMapper">
    <select id="getExpiryDate" resultType="Product">
        select id, mac,expirydate,status,type from product
    </select>
</mapper>